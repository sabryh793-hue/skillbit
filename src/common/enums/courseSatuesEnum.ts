export enum CourseStatusEnum {
    FINISHED = 'finished',
    ACTIVE = 'active',
    LOCKED = 'locked',
}

export enum QuizStatusEnum {
    PASSED = 'passed',
    FAILED = 'failed',
    LOCKED = 'locked',
    UNLOCKED = 'unlocked',
}